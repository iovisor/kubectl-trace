FROM alpine:3.11
# hadolint ignore=DL3018,DL3019
RUN apk add --update \
    bash \
    bc \
    build-base \
    curl \
    elfutils-dev \
    linux-headers \
    linux-lts-dev \
    make

WORKDIR /

COPY /init/fetch-linux-headers.sh /

ENTRYPOINT [ "/fetch-linux-headers.sh" ]
